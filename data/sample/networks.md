# Computer Networks

Computer networks move packets through layered protocols. TCP provides reliable ordered delivery, while UDP favors low overhead. DNS maps domain names to addresses. HTTP is an application protocol commonly transported over encrypted TLS connections.

