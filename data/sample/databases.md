# Databases and Transactions

Relational databases organize records into tables and use indexes to accelerate queries. A B-tree index supports efficient range scans. Transactions provide atomicity, consistency, isolation, and durability. Query planners compare possible execution strategies before selecting a plan.

