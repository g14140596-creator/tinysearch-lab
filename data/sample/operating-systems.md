# Operating Systems

An operating system manages processes, memory, files, and hardware. Virtual memory gives each process an isolated address space. The scheduler decides which runnable process receives CPU time. Synchronization primitives coordinate concurrent threads and prevent data races.

